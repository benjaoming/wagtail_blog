language: python
python:
  - "3.4"
# command to install dependencies
install:
  - pip install -U pip  # required because of setuptools 12
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script:
  - python manage.py test
sudo: false
